<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema escuro -->
    <style name="Theme.QuackPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/quack_blue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/quack_pink</item>
        <!-- Cor do fundo -->
        <item name="backgroundColor">@color/background_dark</item>
        <!-- Cor do texto, aplicada automaticamente -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>